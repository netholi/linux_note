
>/home/anand/.config/sxhkd/
>
>super + alt + m
>~/.local/bin/movies.sh
>
>super + alt + l
>    ~/.local/bin/define.sh
>
>super + alt + k
>    ~/.local/bin/sys
>
super + alt + a
>    ~/.local/bin/apps.sh

>[!info] Tip
>
>Add the path  (add to ~/.bashrc)
>$ export PATH=/home/anand/.local/bin:$PATH
> 
>Add the script to the /home/anand/.local/bin/   folder
>(if needed change the filemanager eg.. pcmanfm or thunar etc in apps.sh)


